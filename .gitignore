# Generated by Cargo
# will have compiled files and executables
/target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# Written by Tam

# Since this project is built and run inside Docker container, the cargo's $CARGO_HOME env variable
# in the container is moved to working directory, which is mapped to host, would be permanent, then
# Cargo won't rebuild everything everytime.

registry
.package-cache